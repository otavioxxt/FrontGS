import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'
import { ThemeProvider } from './contexts/ThemeContext';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <ThemeProvider>
      <App />
    </ThemeProvider>
  </StrictMode>,
)

// notify overlay that React render was attempted
setTimeout(()=>{ if(window.__appHeartbeat) window.__appHeartbeat('mounted (render attempted)'); }, 500);
